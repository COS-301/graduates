import { ApiAuthenticationService } from './api-authentication-service-feature';

describe('apiExampleServiceFeature', () => {
  it('should work', () => {
    //expect(new ApiAuthenticationService()).toEqual('api-example-service-feature');
  });
});
