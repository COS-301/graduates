import { AddPermissionHandler } from './add-permission.handler';

describe('AddPermissionHandler', () => {
  it('should be defined', () => {
    expect(new AddPermissionHandler()).toBeDefined();
  });
});
