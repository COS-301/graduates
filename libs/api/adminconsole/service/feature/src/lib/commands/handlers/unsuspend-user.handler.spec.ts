import { UnsuspendUserHandler } from './unsuspend-user.handler';

describe('UnsuspendUserHandler', () => {
  it('should be defined', () => {
    expect(new UnsuspendUserHandler()).toBeDefined();
  });
});
