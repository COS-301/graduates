import { RemovePermissionHandler } from './remove-permission.handler';

describe('RemovePermissionHandler', () => {
  it('should be defined', () => {
    expect(new RemovePermissionHandler()).toBeDefined();
  });
});
